# n8n-Self-Updater
Daily checks n8n Docker image digest. Sends one email: “Update ready? Reply APPROVE”. Only restarts after you say yes. Zero surprise downtime. 6 nodes, 2-min setup. Works on any VPS. Includes IMAP listener + SSH restart. Sleep easy—n8n updates itself, but only with your OK.
